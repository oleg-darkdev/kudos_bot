// import emodji from '../locales/emodji'






export default function (menuText, emodji)  {
    let menuButtons = [];
    
    
    const {
        emodjiForText: eT,
        emodjiForButtons: eB,
        emodjiForNumbers: eN
    } = emodji;

    menuButtons.push(
        
    )

    return menuButtons
}
